# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!

target 'TestNOUS' do
  
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'RxDataSources'
  
  pod 'Kingfisher', '~> 6.3.1'
  
  target 'TestNOUSTests' do
    inherit! :search_paths
  end
  
  target 'TestNOUSUITests' do
  end
  
end
